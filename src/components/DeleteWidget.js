import React from "react";
import { Button } from "react-bootstrap";

const DeleteWidget = ({ sectionIndex, columnIndex, rowIndex }) => {
  return <Button variant="danger">Удалить виджет</Button>;
};

export default DeleteWidget;
