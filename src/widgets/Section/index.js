import React from 'react';
import {style} from './index.scss';

const Section = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Section.propTypes = {

};

export default Section;
